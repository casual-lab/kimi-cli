Kimi CLI Improvement Proposals

KLIP-4

阶段一：实现pty执行并记录ANSI流

记录ANSI输出流
在关键时刻，支持记录关键点：其实就是ANSI流当前的长度。
输出文件：ANSI流文件bin、关键帧点列表。
这个阶段流程是定死的最简单的TUI交互流程
体现为一个一个单独的python脚本
阶段二：支持从ANSI流和关键帧点，导出关键帧

时空回放功能，一个单独的python脚本，一定是通过HTML方式导出，支持下一步再从HTML导出为图片文件（PNG、JPG）
图片保存位置文件路径能够唯一确定测试id（哪一次测试）、关键帧点（哪一个关键帧）

阶段三：定义测试流程DSL和相应的解析执行器

暂定使用json描述步骤，内容包括：
- 全局meta配置：执行目录、kimi指令参数（就是启动运行tui的终端指令，包括各种flag和对应的值）
- 步骤列表，每一步包括：
  - （需要实现完阶段一之后总结一个可枚举的步骤描述，大概就是键盘、鼠标点击、鼠标滚动、窗口大小等输入信号）